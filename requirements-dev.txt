# Pinned dev/test dependencies (no local install performed)
black==24.1.0
flake8==6.1.0
isort==5.12.0
pytest==7.3.1
mypy==1.5.1

# Install dev + runtime in CI with:
# pip install -r requirements.txt -r requirements-dev.txt